# macOS
.DS_Store
.AppleDouble
.LSOverride

# Editor / OS cruft
*.swp
*~
Thumbs.db

# Node / Next.js
node_modules/
.next/
out/
dist/
build/
.vercel/
.env*.local
next-env.d.ts
*.tsbuildinfo

# Proprietary licensed fonts — local only, not distributed until licensed
# (Adobe Caslon Pro / Irvin Heading Pro / Graphik). Self-hosted via
# next/font/local in app/layout.tsx; must be present in the build env at deploy.
app/fonts/
