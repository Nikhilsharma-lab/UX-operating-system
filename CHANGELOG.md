# CHANGELOG.md

All notable changes to the Nikhil Sharma Executive Knowledge System are recorded here.

The format is loosely based on Keep a Changelog. Dates are absolute.

---

## [Unreleased]

### Changed

- 2026-07-06 — Empirical New Yorker visual-parity rebuild (Decision 065): the design authority is now the measured system in `design-audit/nyer-observed.json` (captured live from newyorker.com), not hand-written design markdown. Regenerated the `app/globals.css` token + type-role layer to measured values (ink #000, hairline #e5e5e5, accent #0879bf, neutral grey family, Irvin/Caslon/Graphik roles at measured sizes, 1312/64px container, 624px article measure). A fresh-context verifier's finding (blue-slate greys) was fixed; its mobile-crash flag was a dev-server transient (prod verified). Fonts left as-is per owner. Root `DESIGN.md` overwritten as the measured contract; legacy `design/` kit marked superseded; `check:type` regenerated to the v4 scale; `CLAUDE.md`/`WORKING-RULES.md`/`ROADMAP.md` added. Side-by-sides + diff in `design-audit/`.

### Added

- 2026-07-08 — Dev-server stability fix (`next.config.mjs`): made the `next dev` file watcher ignore `.next-prod` (plus `.next`, `node_modules`, `.git`). Production QA builds (`NEXT_DIST_DIR=.next-prod`) were writing thousands of files that the running dev server watched, storming Fast Refresh and desyncing the client bundle so homepage `<Link>` navigation stalled ("clicking, nothing moves"). Root-caused via DOM/console inspection (markup correct, routes 200, no overlay); resolved with the watch-ignore + a clean single-server restart. Verified: card links now client-navigate correctly.

- 2026-07-08 — Résumé from CV + homepage card swap (Decision 075): swapped the homepage Recent Projects — CKYC Onboarding promoted to a featured card (with stats), Digital Gold Growth moved to the secondary list. Rebuilt `/resume` so 100% of its content comes from the owner's CV PDF (`lib/resume-cv.ts`, extracted via pypdf), rendered in the dark kamran design (PageShell + article primitives; the PDF's visual design not copied): header + contact, summary, executive-snapshot metric groups, experience (Airtel Payments Bank / PayU India / GE Healthcare), signature work (Fraud Alarm / Transparent Banking / Safe Pay), ventures (LANE / Svenklas), leadership & expertise chips, education, awards, beyond work, clickable contact. Typography normalized to site convention (sentence-case labels, hyphenated dates, middots) with verbatim CV wording. Lint + tsc + prod build pass; no dashes in visible copy; no overflow.

- 2026-07-08 — Site-wide link audit + homepage fixes (Decision 074): crawled all 24 pages + adversarially audited label-to-target correctness. Fixed the only hard 404 (the Résumé PDF) by building a real `/resume` page (dark kamran, from existing public content) and repointing all three Résumé links to it. Wired the LinkedIn/X placeholders (`href="#"` → real profiles, new tab). Retargeted the homepage LANE pill + LANE venture card to the Lane case study (`/cases/lane`). Replaced the homepage identity glyph with the owner's photo avatar (`public/avatar.jpg`, `next/image`). Re-crawl confirms zero dead links and zero `#` placeholders site-wide. Noted: `site-header.tsx`/`site-footer.tsx` are dead (rendered nowhere) — safe to delete. Lint + tsc + build pass.

- 2026-07-08 — Lane founder-built SaaS case (Decision 073): Cases → Founder-built SaaS → Lane, on the flat route `/cases/lane`, authored in the dark kamran design language (single reading column via `PageShell` + shared `article` primitives, matching the rebuilt sibling cases). Registered in the flat index `lib/case-list.ts`; content in `lib/lane-case.ts` (18 blocks). A private-beta founder/product-strategy case: hero + intro, 5-metric strip, founder problem, product thesis (+ surveillance/support principle), the AI Intake Gate wedge, the "built broadly, launched narrowly" hide-decision, what Lane is (+ end-to-end flow), a 4-card product model, the Intake Gate detail (verdicts + gate message + actions + removed soft-escape), anti-surveillance Never/Always columns, what is built, what is hidden for v1 (lifecycle states + deferred list), tech stack, progressive GTM (+ success signals), my role, three founder lessons, sixteen product-system artifact placeholders, related links. Hard rules held: honest private beta, not a traction case — no pricing/team-size/revenue/MRR/ARR/PMF/broad-adoption claims; competitors named (Jira, Linear, Asana, Notion); both comparison lines verbatim; "vibe coding" exactly once, framed seriously; artifacts are placeholders, not dashboards; `/ventures` not linked; no internal-evidence language or dashes. The GPT prompt's visual-design section was intentionally ignored per the owner; only content/structure used. Verified by a two-lens adversarial workflow with a synthesis judge. (Context: Decision 072 — parallel restructure — flattened the Cases IA to `/cases/<slug>` and moved the index source to `lib/case-list.ts`.)

- 2026-07-07 — AI-led Support Automation case + third standalone Big Project (Decision 071): Cases → Big Project → AI-led Support Automation, on a dedicated static route `app/cases/big-project/ai-and-automation`, as a skimmable executive service-design brief (14 tight blocks). A service-experience + automation case (not an app-UI case): redesigning customer issue resolution across IVR, chatbot, email, SMS, and app self-serve journeys via intent mapping, bot workflows, and an Issue Prediction API. Added as a major Big Project (number 04) through the existing `href`/`caseType` direct-link branch; placeholder scaffold renumbered 05-08; the four Safe Second Account cases, both Airtel Big Project cases, and digital-gold untouched. Content in `lib/ai-support-automation-case.ts`: hero + breadcrumb chips + 5 meta rows, a 5-metric strip, the problem, the support-contact→predicted-intent shift, a "what changed" system list + intent chips, an Issue Prediction API block (intro + 9 signals + closing), six experience-decision cards, before/after service flows, four use-case examples + the IVR-acknowledgement pull-quote, twelve service artifact placeholders (labeled "Service artifact", no images), a 4-row metric panel + qualitative CSAT note, my role, five lessons, six related links. Hard rules held: framed as service experience, not app UI (no app-screenshot placeholders, no fake dashboards); no invented CSAT numbers; the high-confidence decision in softened routing language; related links include Transparent Banking and exclude Fraud Alarm; no internal-evidence language or dashes. Copy stays brand-neutral (employer not named). Verified by a three-lens adversarial workflow with a synthesis judge.

- 2026-07-07 — Airtel Payments Bank for Business case + second standalone Big Project (Decision 070): Cases → Big Project → Airtel Payments Bank for Business, on a dedicated static route `app/cases/big-project/airtel-payments-bank-for-business`, as a skimmable executive brief (14 tight blocks). A shipped business-banking app case — consolidating a fragmented merchant + current-account experience into one app, with ecosystem strategy as context; thesis: turn merchant payment acceptance into a deeper business banking relationship. Added as a major Big Project (number 03) via the existing `href`/`caseType` direct-link branch; placeholder scaffold renumbered 04-07; the four Safe Second Account cases, the sibling Airtel Payments Bank App case, and digital-gold untouched. Content in `lib/airtel-payments-bank-for-business-case.ts`: hero + breadcrumb chips + 5 meta rows, a 5-panel metric/context strip, the problem, the business insight (merchants already settling into Current Account), the payment-acceptance→business-banking shift, a 13-item "what shipped" list, seven decision cards (card 7 carries the potential ₹10/month AI-Insights monetization note), before/after model, a research/market-learning block with an "Ease of use for small merchants" design principle + a language/vernacular "not shipped yet" note, twelve caption-led artifact placeholders (no images), a "metrics being tracked" panel with no invented numbers, my role, five lessons, eight related links. Hard rules held: no invented post-launch metrics (shipped recently, metrics still being measured); AI Insights labeled designed/in-development, never live; ₹10/month framed as potential future monetization only; no Mitra reference; no "replaced the old app" claim; no internal-evidence language or dashes (status "Field report v1 · shipped" uses a middot). Brand named intentionally (title/route/thesis), consistent with Decision 069. Verified by a three-lens adversarial workflow with a synthesis judge.

- 2026-07-07 — Airtel Payments Bank App case + standalone Big Project (Decision 069): Cases → Big Project → Airtel Payments Bank App, on a dedicated static route `app/cases/big-project/airtel-payments-bank-app`, as a skimmable executive brief (15 tight blocks) about research + product strategy + controlled decoupling from Airtel ThanksApp. First standalone single-deep-case Big Project: not nested under Safe Second Account, no subprojects. Extended `CaseBigProject` with optional `href`/`caseType` so `/cases` renders it as a major Big Project (number 02) with a "Read case study" CTA linking straight to its route, and the homepage preview shows its case type instead of a subproject/case count; placeholder projects renumbered 03-06. The four Safe Second Account case cards untouched. Content in `lib/airtel-payments-bank-app-case.ts`: hero + breadcrumb chips + meta, 5-metric outcome strip, problem, strategic question, controlled-decoupling shift, research (methods + 60-70-user sample + four user quotes), 15-item "what shipped" + 1% rollout note, the "Homepage as business real estate" construct (six buckets + "Pixel per dollar"), six decision cards, before/after model, fourteen caption-led placeholder tiles (no images), a validation panel (rows + success signals + hedged 1%-rollout learnings + numberless business-case note), my role, five lessons, seven related links. Thesis preserved: ThanksApp stayed the acquisition engine, the bank app became the engagement engine, both coexist — never framed as a replacement. Public-safety: brand named intentionally here (case title/route/thesis); no internal financial numbers; "beta" kept out of the hero (status "Field report v1"); 1% rollout stated carefully; no internal-evidence language or dashes. Verified by a three-lens adversarial workflow with a synthesis judge.

- 2026-07-07 — Fourth real nested case study, Welcome Kit (Decision 068): Safe Second Account → Activation → Welcome Kit, on a dedicated static route, as a skimmable executive brief (~1,030 words incl. captions, same house layout as the sibling cases). New Activation subproject (04) under Safe Second Account holding one Welcome Kit card; Fraud Alarm, Transparent Banking, and CKYC Onboarding untouched. Content in `lib/welcome-kit-case.ts`: hero + breadcrumb chips, a 5-metric outcome strip, the problem/shift (account opened ≠ activated → daily-use activation), a "what shipped" block with two lists (Welcome Kit experience + Set and Forget flow), six decision cards, before/after flows, a "Why Set and Forget mattered" Safe Second Account logic callout, thirteen caption-led placeholder tiles (no images), a metric panel (primary goal + 6 rows), my role (2 intro + 6 bullets), five lesson cards, and related links. Deliberately not oversold: no industry-first or revolutionary framing; no exact uplift claimed for UPI setup / transactions / retention (only the 80-85% Welcome Kit open rate "among users who saw the impression" and 75-80% install rate "after offline onboarding" are stated). Public-safety clean: employer anonymized to "the bank"; "@airtel" handle and consumer-app brand names generalized; UPI mandate named for auto-load; no persona, internal language, invented dates/dashboards, or dashes. Verified by a three-lens adversarial workflow with a synthesis judge.

- 2026-07-07 — Third real nested case study, CKYC Onboarding (Decision 067): Safe Second Account → Digital Onboarding → CKYC Onboarding, on a dedicated static route, as a skimmable executive brief (~1,105 words, same house layout as Fraud Alarm / Transparent Banking). New Digital Onboarding subproject (03) under Safe Second Account holding one CKYC Onboarding card; Fraud Alarm and Transparent Banking untouched. Content in `lib/ckyc-onboarding-case.ts`: hero + breadcrumb chips, a 5-metric outcome strip, the problem/shift (Video KYC → 24/7 CKYC/CERSAI automation), an 8-item "what shipped" hub, seven decision cards, before/after 9-step flows, thirteen caption-led placeholder tiles (no images), a metric panel (primary + 7 rows), my role (1 intro + 10 bullets), five lessons, and related links. No future / "what I'd improve now" block. Public-safety clean: employer anonymized to "the bank"; CAC shown only as "approximately 70-75%" (no rupee CAC values); no launch date; "one of the first in the industry" appears once; no persona, banned names, internal language, or dashes.

- 2026-07-06 — Second real nested case study, Transparent Banking (Decision 066): Safe Second Account → Trust → Transparent Banking, on a dedicated static route, as a skimmable executive brief (~955 words, 13 tight blocks). New Trust subproject under Safe Second Account; Fraud Alarm untouched. Content in `lib/transparent-banking-case.ts`; screens are compact placeholder tiles (no images). Metric scoped to exposed users; public-safety clean ("onboarding consent" neutralized to "account-opening consent").

- 2026-07-06 — First real nested case study, Fraud Alarm (Decision 063): Safe Second Account → Safety → Fraud Alarm, on a dedicated static route `app/cases/safe-second-account/safety/fraud-alarm`, written as a skimmable executive brief (12 blocks, ~600 words, ~65% shorter than a full-length case). Safe Second Account leads `/cases` as the real Big Project ahead of the placeholder scaffold; content in `lib/fraud-alarm-case.ts`. Screenshots are compact placeholder tiles (owner to replace); no images imported. Public-safety clean (no persona, banned names, real metrics, internal language, or dashes); "approximately 15 minutes"/"under 3 minutes" and the 39% signal preserved. Digital-gold route untouched.

- 2026-07-06 — Nested Cases IA prototype (Decision 062): added a subproject layer — `/cases` is now Big Project → Subproject → Case Study (4 / 6 / 11 placeholders), subprojects shown in indented left-rule bands. New `app/cases/[slug]/[subSlug]/[caseSlug]` placeholder route (13-section "Content pending" shell); the previous flat `[slug]/[caseSlug]` route removed (would clash with the new `[subSlug]` level). `lib/cases.ts` rebuilt with `CaseBigProject`/`CaseSubproject`/`CaseStudy` (existing full-case type renamed `FullCaseStudy`). Homepage preview shows subproject + case counts. Digital-gold route preserved but unfeatured. Public-safety scan clean; six screenshots + review in `WEBSITE/REVIEW/cases-ia/`.

- 2026-07-06 — Cases IA prototype (Decision 061): `/cases` rebuilt as a library of four big product systems with nested placeholder case cards (10 total); new `app/cases/[slug]/[caseSlug]` placeholder case landing pages showing the 13-section case-report shell (all "Content pending"); homepage Cases preview switched to high-level big-project cards linking only to `/cases`. Nested data model in `lib/cases.ts` (`caseProjects`/`CaseCard`/`CASE_SHELL`); `caseSlots`/`CASE_STRUCTURE` removed. Existing `/cases/digital-gold-growth` route preserved but no longer featured. No real names, metrics, or confidential content; public-safety scan clean. Screenshots + review in `WEBSITE/REVIEW/cases-ia/`.

- 2026-07-06 — Executive Snapshot to four cards (Decision 060): removed the Outcomes card (redundant with the adjacent Selected Outcomes strip), fixing the cramped 5-column grid — cards go 262→332px wide and headings fit one line. Card text rebuilt to the New Yorker "Today's Mix" hierarchy (`.t-mix-hed` Caslon 22/28 near-black + `.t-mix-dek` Caslon 17/24 #333, measured 1:1 from a local crawl), subheadings aligned on one baseline. Leadership heading tightened to "Built the design function".

- 2026-07-06 — Page shell widened to 1472px (Decision 059): single-token change (`--spacing-page-max`), sitewide via `.page-shell`; reading measures unaffected (element-capped); hero fold-fit and snapshot baseline verified at 1600×900. Executive Snapshot plates now use the five owner-supplied photographs (Decision 058 amendment).

- 2026-07-05 — Executive Snapshot editorial gallery (Decision 058): the five snapshot cards became open staggered columns per the owner's reference — rubric + short extractive heading above each placeholder plate, plate bottoms flush on a shared baseline, descriptions below; centered section header (`align` prop on SectionShell); `portrait`/`tall` aspects added to EditorialArt; five `home.snapshot.*-plate` slots registered in ART-DIRECTION.md.

- 2026-07-05 — Masthead matched to hero (Decision 057): header flipped to the hero's near-black field with white Irvin wordmark and muted-paper Graphik nav; mobile panel matched; DESIGN.md masthead rule updated (supersedes the white masthead of 032/033).

- 2026-07-05 — Homepage IA cleanup (Decision 056): identity grid reclassified as Executive Snapshot (ledger removed from inside it, card copy tightened); Selected Outcomes added as a compact evidence-gated metric strip (seven public-approved metrics); standalone Capabilities removed from homepage + nav; belief section relabeled Operating Thesis with new right-column lines; library band split into standalone Cases (before Transformations) and Operating Manual (after) sections. New components `selected-outcomes.tsx`, `cases-section.tsx`, `manual-section.tsx`; removed `capabilities-section.tsx`, `library-band.tsx`. Screenshots + review in `WEBSITE/REVIEW/homepage-ia/`.

- 2026-07-05 — Thinking section decoupled (Decision 055): Thinking left the library band (whose sticky case rail was scrolling into it) and became its own full-viewport section — bone tint band, user-supplied sticker-album illustration as the left plate (`public/art/thinking-album.jpeg`), centered Irvin title and Caslon paragraph right; essay seeds shipped first as one flowing paragraph, then as a hairline-river list, and landed on the fiction-page grammar (same day): rubric + Irvin title above a maximized plate with a byline-position dek beneath (left), drop-cap Caslon intro + seed river under an accent rubric header (right). Library band is now Operating Manual + case rail only. New component `thinking-section.tsx`. Guards pass (lint, build, check:type, zero-dash audit).

- 2026-07-05 — Hero feature opener (Decision 054): hero image replaced with the user-supplied tile-grid face illustration (`public/art/hero-face-grid.jpeg`); hero recomposed on a near-black field in the New Yorker feature-title style — centered Irvin headline (new `t-display-feature` role class) and centered white Caslon dek; hero eyebrow and plate caption removed. Type guard (`check:type`) passes; DESIGN.md and ART-DIRECTION.md synced.

- 2026-07-05 — Homepage recomposition (Decision 053): magazine front-page grammar. White Irvin masthead shipped (black hairline rule; dark bar retired sitewide); hero recomposed as a white magazine lead with the doorway illustration as a captioned art plate; numbered section eyebrows retired in favor of the rubric voice (indices only where sequence is real); Cases + Operating Manual + Thinking merged into a river+rail library band with forthcoming content designed as intentional editorial objects (no more "Content pending" cards or "Image slot" panels on the homepage); transformations re-hierarchized as lead story + two secondary; ventures given illustrated plates; evolution compacted to a timeline strip; calm structural motion layer added (hero load sequence + below-fold entrances, reduced-motion safe). Components: `library-band.tsx` and `reveal.tsx` added; `cases-preview.tsx`, `operating-manual-preview.tsx`, `thinking-preview.tsx` removed. Guards pass: lint, production build, `check:type`.

- 2026-07-05 — Typography scale v3 (Decision 052): re-tokenized the entire type system against measured newyorker.com values. Role classes (`.t-display` → `.t-caption`) in `app/globals.css` are now the single source of truth; ~40 arbitrary `text-[Npx]` component values and every `font-light` (faux 300 weight) purged; support-text floor raised 13→15px; deks unified at Caslon 21/28; in-article section headings 21→28px; hero capped 86→64px; page H1s capped at 42px w400; paragraph rhythm 24px. Regression guard added: `npm run check:type` (scripts/check-type.mjs, 12 computed-style probes on 3 key pages against the production build). `DESIGN.md` Type Scale section rewritten with the measured reference table.

- 2026-06-28 — Initial documentation architecture for the Executive Knowledge System.
  - Root files: `README.md`, `PROGRAM_INDEX.md`, `MASTER_PLAYBOOK.md`, `AI_COLLABORATION_MODEL.md`, `DESIGN.md`, `DECISION_LOG.md`, `CHANGELOG.md`, `BACKLOG.md`, `ANTI_GOALS.md`, `VOCABULARY.md`, `CONTENT_GRAPH.md`.
  - Folder system: `FOUNDATION/`, `WEBSITE/`, `PAGES/`, `TRANSFORMATION_STORIES/`, `OPERATING_SYSTEM/`, `THINKING/`, `FRAMEWORKS/`, `BUILDING/`, `WRITING/`, `MEDIA/`, `GROWTH/`, `QUALITY/`, `RELEASES/`.
  - Drift-protection systems: `EVIDENCE/`, `CAREER_ASSETS/`, `PROMPTS/`, `AUDITS/`, `GOVERNANCE/`, `FUTURE/`.
  - `styles/globals.css` — Tailwind v4 theme tokens, `:root` variables, and base/utility layers (not yet wired; awaiting app scaffold).
  - Decision Log seeded with decisions 001–008.

### Notes

- This release is documentation scaffolding only. No UI, components, or packages were created.
